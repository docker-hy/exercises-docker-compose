name: simple_server_with_browser tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run simple_server_with_browser tests
        run: bash tests/test_2_02_simple_server_with_browser.sh
